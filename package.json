{
  "name": "xhf-blob",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:cf": "turbo run build --filter=blog",
    "lint": "turbo lint",
    "start": "pnpm --filter blog start"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.18.0"
  }
}
